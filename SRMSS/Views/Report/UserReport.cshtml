@model IEnumerable<SRMSS.Models.User>

@{
    Layout = null;
}

<style>
    body {
        font-family: Arial;
        margin: 20px;
        font-size: 12px;
    }

    h1, h2, h3, p {
        text-align: center;
    }

    table {
        width: 100%;
        border-collapse: collapse;
    }

    th {
        background-color: #0d6efd;
        color: white;
        padding: 8px;
        text-align: center;
    }

    td {
        border: 1px solid #ddd;
        padding: 6px;
        text-align: center;
    }

    .footer {
        text-align: center;
        margin-top: 20px;
    }
</style>

<div class="mb-3">
    <a asp-action="Index" class="btn btn-secondary"> ← Back </a>

    <a asp-action="UserReportPDF" class="btn btn-danger">
        Download PDF
    </a>

</div>

<div class="text-center mb-4">
    <h1 style="color:#0d6efd;"> SMART ROUTE MANAGEMENT SYSTEM </h1>

    <h3>
        User Management Report
    </h3>

    <p>
        Generated Date :
        @DateTime.Now.ToString("dd/MM/yyyy hh:mm tt")
    </p>

    <hr />

</div>

<div> Total Users : <strong>@Model.Count()</strong> </div>

<br />

<table>
    <thead> <tr> <th>User ID</th> <th>Full Name</th> <th>Username</th> <th>Role</th> <th>Phone Number</th> <th>Email</th> <th>Status</th> </tr> </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.UserID</td>
                <td>@item.FullName</td>
                <td>@item.Username</td>
                <td>@item.Role</td>
                <td>@item.PhoneNumber</td>
                <td>@item.Email</td>
                <td>@item.Status</td>
            </tr>
        }
    </tbody>

</table>

<div class="footer">
    <hr />

    <p>
        Generated by SRMSS Reporting Module
    </p>

    <p>
        © 2026 Smart Route Management System
    </p>

</div>